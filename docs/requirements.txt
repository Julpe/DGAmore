# Dependencies to build the Sphinx documentation.
# mpi4py, cupy and psutil are intentionally omitted: they are mocked in conf.py
# (autodoc_mock_imports), so the docs build needs neither MPI nor CUDA.
# Pinned: the pydata theme's navbar/sidebar options and layout change across minor releases, so an unpinned
# upgrade can silently shift the rendered docs. Bump these deliberately and re-check the navbar/sidebar locally.
sphinx~=9.1
pydata-sphinx-theme~=0.19.0

# Runtime imports that autodoc must resolve while importing the package.
numpy~=2.2
scipy~=1.18
h5py~=3.16
pandas~=3.0
matplotlib~=3.10
multimethod~=2.0
ruamel.yaml~=0.19
threadpoolctl~=3.6
