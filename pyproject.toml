[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120

[tool.coverage.run]
omit = [
    "*/DGAmore.py",
    "*/plotting.py",
    "*/config_parser.py",
    "*/symmetrize.py",
    "*/ana_cont.py",
    "*/dmft_interface.py",
]